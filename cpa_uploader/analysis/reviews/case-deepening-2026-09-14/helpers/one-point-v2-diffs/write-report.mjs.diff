diff --git a/cpa_uploader/analysis/reviews/case-deepening-2026-09-14/helpers/one-point-v2-source/write-report.mjs.txt b/cpa_uploader/analysis/reviews/case-deepening-2026-09-14/helpers/write-report.mjs
index 4c113258..dad2cc58 100644
--- a/cpa_uploader/analysis/reviews/case-deepening-2026-09-14/helpers/one-point-v2-source/write-report.mjs.txt
+++ b/cpa_uploader/analysis/reviews/case-deepening-2026-09-14/helpers/write-report.mjs
@@ -2,6 +2,7 @@ import fs from 'node:fs';
 import assert from 'node:assert/strict';
 import {createHash} from 'node:crypto';
 import {parseReportArgs,assertFinalChecksPassed} from './final-check-paths.mjs';
+import {validateQaBank} from './representative-qa.mjs';
 
 const R='cpa_uploader/analysis/reviews/case-deepening-2026-09-14';
 const D='cpa_uploader/drafts/case-deepening-2026-09-14';
@@ -66,6 +67,9 @@ assert.equal(grade.within_tolerance_ratio,grade.within_tolerance/grade.fixed_eva
 assert(grade.within_tolerance_ratio>=0.95);
 assert.equal(grade.outside_tolerance.length,grade.scores.filter(score=>!score.within_tolerance).length);
 assert(grade.scores.every(score=>ids.includes(score.source_set_id)));
+const representativeScope=validateQaBank(sets,read(R+'/case-qa.json'));
+assert.deepEqual(read(R+'/representative-scope-check.json'),representativeScope);
+assert.equal(grade.fixed_evaluated_answers,representativeScope.planned_evaluated_answers);
 
 const old=read(R+'/integration-baseline/bank.json');
 const oldCatalog=read(R+'/integration-baseline/catalog.json').classifications;
@@ -96,6 +100,7 @@ const lines=[
   '기출문제와 고급회계감사연습의 지문·물음·해설, 직접 기준서 원문을 대조하고 현재 은행 및 진행 중 초안과 차이를 검토했다. 기출·모의·연습 수록을 임의로 합산하거나 교재 재수록을 새 출제로 세지 않았다. 적용 판본과 원문 위치는 각 사례의 설계·검토 장부에 기록했다.','',
   `총 ${shape.new_questions}개 물음에 대해 사실 의존성, 발문과 모범답안, 독립 득점 요건·정수 부분점수, 공식 근거·판본, 주제와 기존 문항 차이를 실제 agent가 대조했다. 미해결 내용 결함은 없으며, 이를 사람의 직접 내용 확인 또는 별도 API 의미검수로 기록하지 않았다.`,'',
   '## 실제 채점','',
+  `대표 구성은 모범답안 ${representativeScope.model_answers}개·대표 부분정답 ${representativeScope.partial_answers}개·대표 오답 ${representativeScope.wrong_answers}개다. 1점 물음 ${representativeScope.one_point_questions}개는 0점과 1점 사이의 정수 부분점수가 없어 대표 부분정답이 적용되지 않는다. 해당 물음은 모범답안과 정오 경계의 대표 오답을 실측하며 배점을 늘리거나 부분점수를 만들지 않았다.`,'',
   `고정된 ${grade.fixed_evaluated_answers}개 대표 답안(저장 모범·대표 부분·대표 오답)을 ${grade.requests}개 사례 단위 요청으로 통합했다. 기대점수와 정확히 일치한 답안은 ${grade.exact_score_matches}개, ±1점 이내는 ${grade.within_tolerance}개(${(grade.within_tolerance_ratio*100).toFixed(2)}%)다. 0점이 아닌 허용 편차는 ${deviations.length}개, 허용 범위 밖은 ${grade.outside_tolerance.length}개다. 원 기대값·실측·원응답을 보존했다. 이 비율은 고정된 대표 답안의 관측 결과이며 통계적 신뢰수준이나 모든 답안의 정확도 보장이 아니다.`,'',
   `실제 SDK 호출은 ${grade.actual_sdk_calls}개다. ${cost} 단가는 2026-09-12 확인 기록을 고정한 추정용 값이며 청구서·세금과 다르다. 입력·캐시·출력 토큰과 요청·응답 식별자는 실행 기록에 보존했다. 이번 금액 정책은 예산 ${grade.budget_usd===null?'미지정':`$${grade.budget_usd}`}·${grade.budget_enforcement}이며 모델은 Luna를 유지했다.`,'',
 ];
