import fs from 'node:fs';
import assert from 'node:assert/strict';
import {createHash} from 'node:crypto';
const R='cpa_uploader/analysis/reviews/standard-points-implementation-2026-09-14';
const read=f=>JSON.parse(fs.readFileSync(f));
const ref=file=>({file,sha256:createHash('sha256').update(fs.readFileSync(file)).digest('hex')});
const bank=read('cpa_uploader/data/cpa_question_sets_v3.authoring.json'),catalog=read('cpa_uploader/data/learning-question-classifications.json');
const before=read(R+'/concurrent-additions-v1/bank.snapshot.json'),route=read(R+'/publication-review-routing-v2.json');
const installed=read(R+'/publication-v3/install-completion.json'),deployed=read(R+'/db-publication-v5/completion.json');
assert.equal(installed.status,'canonical_installed_and_validated');assert.equal(deployed.status,'production_published_and_independently_verified');
assert.equal(read(R+'/retirement-history-verification-v1/after.json').status,'passed');
assert.equal(read(R+'/final-checks-v2/completion.json').status,'pass');
for(const id of ['coverage','analysis-build','analysis-check','review-preservation'])assert.equal(read(R+'/final-checks-v1/'+id+'.json').exit_code,0);
const scopeAudit=read(R+'/db-grading-scope-postdeployment.json');
assert.equal(scopeAudit.verdict,'pass');assert.equal(scopeAudit.expected.scope_fields,44);
assert.equal(scopeAudit.expected.scope_bearing_sets,19);assert.equal(scopeAudit.release.id,deployed.release_id);
assert.equal(read(R+'/evidence-archive-followup-resolution.json').status,'pass');
for(const v of [4,5,6])assert.equal(read(R+'/sealed-v'+v+'/readiness.json').ready,true);
const summaries=[1,2,3].map(v=>({file:R+'/sealed-v'+v+'/batch.json',summary:read(R+'/sealed-v'+v+'/summary.json')}));
const finalScores=summaries.flatMap(({file,summary})=>summary.scores.filter(s=>route.entries.some(r=>r.set_id===s.source_set_id&&(r.original_evidence?.file??r.evidence.file)===file)));
assert.equal(finalScores.length,579);assert(finalScores.every(s=>s.within_tolerance));
const all=summaries.flatMap(x=>x.summary.scores),calls=summaries.reduce((n,x)=>n+x.summary.actual_sdk_calls,0),known=summaries.every(x=>x.summary.known_cost);
const standard=catalog.classifications.filter(m=>m.question_style==='standard');
const find=(s,q)=>bank.find(x=>x.id===s).subquestions.find(x=>x.id===q);
const distribution={};for(const m of standard){const p=find(m.source_set_id,m.subquestion_id).criteria.length;distribution[p]=(distribution[p]??0)+1;}
const totalPoints=sets=>sets.reduce((n,s)=>n+s.subquestions.reduce((n,q)=>n+q.criteria.reduce((n,c)=>n+c.max_points,0),0),0);
const result={created_at:new Date().toISOString(),status:'complete',bank:ref('cpa_uploader/data/cpa_question_sets_v3.authoring.json'),catalog:ref('cpa_uploader/data/learning-question-classifications.json'),release_id:deployed.release_id,counts:{standard_questions:standard.length,standard_points:standard.reduce((n,m)=>n+find(m.source_set_id,m.subquestion_id).criteria.length,0),case_questions:catalog.classifications.length-standard.length,whole_bank_questions:catalog.classifications.length,sets:bank.length,points:totalPoints(bank),distribution},grading:{model:'gpt-5.6-luna',final_questions:193,final_answers:finalScores.length,final_exact:finalScores.filter(s=>s.delta===0).length,final_within_tolerance:finalScores.filter(s=>s.within_tolerance).length,all_calls:calls,all_observations:all.length,all_within_tolerance:all.filter(s=>s.within_tolerance).length,preserved_initial_outside_tolerance:1,new_semantic_api_calls:0,human_review_performed:false,known_cost:known,estimated_cost_usd:known?summaries.reduce((n,x)=>n+x.summary.estimated_cost_usd,0):null},publication:{revised_or_derived_sets:route.entries.length,actual_review_sets:route.entries.filter(r=>r.method==='efficient').length,derived_sets:route.entries.filter(r=>r.method==='subset').length,preserved_concurrent_case_sets:6,retired_active_set_ids:read(R+'/candidate-v3/retired-sets.json')},evidence:[R+'/publication-v3/install-completion.json',R+'/db-publication-v5/completion.json',R+'/db-publication-v5/verification.json',R+'/targeted-clarification.json',R+'/residual-findings-v1.json',R+'/coverage-final-candidate.json'].map(ref)};
assert.equal(result.counts.standard_questions,372);assert.equal(result.counts.standard_points,1400);
result.evidence.push(...[R+'/db-grading-scope-postdeployment.json',R+'/retirement-history-verification-v1/after.json',R+'/db-retirement-migration-v1/completion.json',R+'/final-checks-v2/completion.json',R+'/subset-cache-tests.json',R+'/code-checks/tests.json'].map(ref));
result.evidence.push(...[R+'/evidence-archive-followup-resolution.json',R+'/publication-review-routing-v2.json',...[4,5,6].map(v=>R+'/sealed-v'+v+'/readiness.json')].map(ref));
result.evidence.push(...[R+'/reuse-acceptance-completion.json',R+'/db-publication-v5/preparation.json',R+'/db-publication-v5/receipt.json',R+'/db-publication-v5/roundtrip.json',R+'/pgp-transport-probe-v2/completion.json'].map(ref));
result.verification={production_scope_fields:44,scope_bearing_sets:19,historical_table_snapshots_preserved:12,initial_code_tests:{total:592,passed:591,failed:1,reason:'과거 보조 분석 입력의 재생성으로 경로 바이트가 변경됨. 당시 보존본을 엄격히 해석하는 후속 수정과 표적 재검증으로 해결.'},analysis_and_wiki:'passed'};
fs.writeFileSync(R+'/completion.json',JSON.stringify(result,null,2)+'\n',{flag:'wx'});
const text=`# 기준서형 배점 검토 반영 결과\n\n정본·공개본·암호화본과 운영 DB에 분리·통합·배점·부분정답 기준을 반영했다. 운영 release는 \`${deployed.release_id}\`이며 원문 바이트·공개 payload·학습 분류의 DB 왕복 검증을 통과했다.\n\n| 항목 | 반영 전 | 반영 후 |\n| --- | ---: | ---: |\n| 기준서형 물음 | 354 | 372 |\n| 기준서형 총배점 | 1,409 | 1,400 |\n| 1점 물음 | 24 | 4 |\n| 최대 물음 배점 | 12 | 8 |\n| 전체 물음(동시 추가 사례 포함) | ${before.reduce((n,s)=>n+s.subquestions.length,0)} | ${catalog.classifications.length} |\n| 전체 총배점 | ${totalPoints(before)} | ${totalPoints(bank)} |\n\n기존 164물음을 재편하여 독립 물음 182개를 만들고, 나머지 기준서형 190물음의 내용은 유지했다. 다른 작업에서 추가한 사례형 6세트·18물음과 해당 검수 이력도 그대로 보존했다. 최종 사례형은 156물음이다.\n\n## 부분정답과 분리·통합\n\n독립적으로 인정할 수 있는 의미마다 1점을 부여하여 충족한 기준을 합산한다. 같은 뜻이나 대안 표현을 중복 배점하지 않고, 0.5점으로 쪼개지 않는다. 판단·근거는 독립 기준으로 평가하며 답안의 근거가 결론을 분명히 함축하면 판단도 인정한다.\n\n- 거래·사건의 경영진주장: 12점 한 물음을 6·6·3·4점의 네 물음으로 분리했다. 명칭과 정의를 구별하고 정의 일부의 독립 부분정답을 인정한다.\n- 감사인측 전문가 업무 평가: 10점을 발견사항·결론 3점, 가정·방법 4점, 원천데이터 3점으로 분리했다.\n- 기타정보 단락의 구성요소: 10점을 두 개의 5점 물음으로 분리했다.\n- 남은 1점 물음 네 개는 단일 개념·원칙 자체를 묻는 경우로 유지했다.\n\n## 실제 채점과 수정\n\nLuna로 총 ${calls}회 호출했다. 최초 576개 답안 중 575개가 ±1점 이내였고, 검수 계승이 불가능한 잔존 물음의 추가 3개 답안도 정확히 일치했다.\n\nIT 응용프로그램·기타 IT 환경 식별의 부분답안은 기대 2점인데 최초 0점이 나왔다. 기준서·발문·답안과 실행 당시 규칙을 다시 대조하여 발문의 전제 반복을 요구한 과소채점으로 판단했다. 원 답안과 기대 2점을 보존하고, 해당 물음의 전제와 두 criterion의 허용 범위를 명확히 했다. 후속 실측은 모범 4점·부분 2점·오답 0점이 모두 정확히 일치했다.\n\n최종 판본에 연결한 대표답안 ${finalScores.length}개는 모두 ±1점 이내이며 ${result.grading.final_exact}개가 정확히 일치했다. 최초 실패를 포함한 전체 ${all.length}관측 중 ${result.grading.all_within_tolerance}개가 ±1점 이내이다. 원 실패·원 기대값·원 응답과 사용량은 삭제하지 않았다. 이는 관측된 채점 일관성이며 모든 가능한 답안의 정확도나 통계적 신뢰수준을 뜻하지 않는다.\n\n193세트는 이번 실제 검토·대표 채점 근거를 사용하고, 삭제 후 내용이 그대로 남은 50세트는 원 승인·분류·답안·채점 입력의 동일성을 검증해 근거를 계승했다. 새 모델 의미검수 API나 사람의 대리 검수로 기록하지 않았다.\n\n## 반영·보존 근거\n\n- [물음별 변경 계보](../../${R}/candidate-v3/lineage.json)\n- [정본·공개본 설치 검증](../../${R}/publication-v3/install-completion.json)\n- [운영 DB 독립 검증](../../${R}/db-publication-v5/verification.json)\n- [기존 검토 보고서](standard-points-2026-09-14.md)\n\n원세트 55개의 active release 제외는 사용자 승인 목록과 기대 원릴리스에 결속한 경로로 처리하며 과거 버전·학습·채점 기록은 보존한다. 세부 검증·게시 단계 및 실행 실패/후속 수정은 [작업 장부](../../${R}/README.md)에서 구별한다.\n`;
const checksText=`\n## 운영 전달과 코드 검증\n\n실제 운영 DB의 19세트·44개 범위 조건을 비공개 문항 조회 → 학습 물음 → 채점 입력까지 대조했다. 기존 12개 테이블의 역사행 보존 검사와 승인된 55세트의 정확한 비활성화 검사를 통과했다. 분석·보존·wiki 생성/검사도 통과했다.\n\n전체 코드 검사 최초 실행은 592개 중 591개 통과·1개 실패였다. 실패는 기존 사례 문항의 검수 보조 분석 파일이 재생성된 문제로, 당시 실행에 함께 봉인된 수집 기록과 동일 해시 보존본을 확인하는 경로를 추가했다. 실제 문항 출처·답안·실측 입력의 현재 검증은 유지했다. 후속 표적 회귀 62개가 통과했으며 전체 테스트를 다시 실행한 결과는 아니다. 원 실패 로그와 후속 회귀 결과는 작업 장부에 각각 보존했다. 삭제 계승 회귀 9개와 퇴역 DB 회귀 2개, 타입·대상 lint 검사도 통과했다.\n\n실제 반환 사용량에 고정 단가를 적용한 전체 호출 비용 추정은 약 $${result.grading.estimated_cost_usd.toFixed(6)}이다. 청구서 확정액과 구별하며 원 응답의 입력·캐시·출력 토큰과 요청 식별자는 각 실행에서 확인할 수 있다.\n`;
fs.writeFileSync('docs/reports/standard-points-implementation-2026-09-14.md',text+checksText,{flag:'wx'});
fs.writeFileSync(R+'/README.md',`# 기준서형 배점 검토의 정본·공개본·운영 반영\n\n[결과 보고서](../../../../docs/reports/standard-points-implementation-2026-09-14.md) · [최종 결과](completion.json)\n\n## 수동 입력\n\n- 사용자 승인: authorization.md, baseline.json\n- 물음별 재편 명세: plan-01-05.json, plan-06-10.json, plan-11-14.json, plan-15-19.json\n- 독립 검토: qa-*.json 및 후속 resolution, source-evidence-11-14.json\n- 추가 잔존 검수: subset-regrade-representatives.json, subset-additional-representatives.json\n- 실측 오류의 후속 명료화: targeted-clarification.json, residual-findings-v1.json\n- 관계 재연결: coverage-final-candidate.json 및 update-coverage.mjs의 수동 매핑\n\n## 생성 결과와 실행\n\n- candidate-v1: 최초 통합. candidate-v2: 한 물음의 전제·scope 명료화. candidate-v3: 동시 추가된 사례 6세트 보존 통합.\n- execution-v1/ sealed-v1/: 192물음·576답안. execution-v2/ sealed-v2/: 잔존 1물음·3답안. execution-v3/ sealed-v3/: 명료화 1물음·3답안. 각 원 manifest·응답·사용량·판정은 해당 버전으로 보존한다.\n- subset-batch-v1/: 51개 사전검사 중 50개 통과·1개 새 실측 필요. subset-batch-v2/: 통과 50개. 이후 성능 검증은 후속 evidence로 구별한다.\n- publication-v1/: 게시 전 상태 점검 인자 오류로 격리 stage 실패. 정본·DB 쓰기 0. 당시 코드·로그 보존. publication-v3/: 수정 후 엄격 검증·설치.\n- concurrent-additions-v1/: 작업 중 추가된 사례와 당시 원릴리스의 읽기 전용 보존본.\n- db-publication-v5/: 승인 퇴역·원릴리스 결속과 운영 DB 적용·독립 검증.\n- code-checks/: 타입·테스트 결과. 중단되거나 외부 병렬 변경에 영향을 받은 실행은 별도 evidence로 보존하며 통과로 표시하지 않는다.\n\n## 재현 범위\n\n정적 후보 생성기는 저장 수동 입력과 해당 버전 원본을 사용한다. publish/deploy는 현재 파일·입력·기대 원릴리스 가드가 있는 실제 변경 도구이며 이전 실행 폴더를 덮어쓰지 않는다. 과거 API 응답을 새 호출로 재현했다고 기록하지 않는다. 키와 계정 자격증명은 장부에 저장하지 않는다.\n`,{flag:'wx'});
fs.appendFileSync(R+'/README.md','\n## 후속 수락·운영 검증\n\n- publication-v2/: 검수 코드 변경을 엄격한 새 수락이 거절하여 쓰기 전에 중단했다. failure-analysis.json과 당시 runtime을 보존한다.\n- execution-v4/5/6 및 sealed-v4/5/6/: 원 v1/2/3 관측을 새 수락 코드로 재처리했다. 추가 API 0회이며 원 manifest·답안·기대값·사용량은 원 실행에 남는다. reuse-acceptance-completion.json과 publication-review-routing-v2.json에 연결한다.\n- evidence-archive-followup*.json: 당시 실행에 봉인된 수집 기록과 동일 SHA의 보조 분석 보존본 해석, 원 6개 receipt·54답안 동일성과 후속 회귀 62개·타입 검사.\n- retirement-history-verification-v1/after.json: 55세트의 정확한 비활성화와 기존 12개 테이블 역사행 보존.\n- db-grading-scope-postdeployment.json: 실제 운영 19세트·44개 scope의 비공개 RPC→학습 투영→채점 입력 전달.\n- final-checks-v1/: coverage 적용·분석 생성/검사·과거자료 보존 통과와 첫 wiki 실패. final-checks-v2/: 퇴역 페이지 보존·생성 목차 보완 후 최종 wiki 검사.\n');
fs.appendFileSync(R+'/README.md','\n## 운영 DB 전송과 후속 실행\n\n- db-publication-v1/: 은행 전체 검증은 통과했으나 PostgREST 트랜잭션 시간 제한으로 등록이 취소됐다. 원 응답과 실패를 보존했다.\n- db-publication-v2/: Management API 요청 크기 제한으로 HTTP 413이 발생했다. db-publication-v3/4는 크기를 줄이는 읽기 전용 사전검사에서 같은 제한을 확인했으며 등록을 실행하지 않았다. 실패 후 기존 활성 릴리스와 역사행이 그대로임을 확인했다.\n- pgp-transport-probe-v2/와 db-publication-v5/: 원 payload를 압축 전송하고 기존 DB 함수로 복원했다. 공개 상수를 사용하는 전송 용기이며 비밀 보호 수단으로 취급하지 않는다. 원 바이트 SHA와 PostgreSQL JSONB SHA, 정본 원문 SHA가 일치하는 경우에만 기존 퇴역 승인 RPC를 실행했다. service_role과 120초 제한은 해당 트랜잭션에만 적용했으며 기본 설정과 권한은 그대로다.\n- v5의 원 응답·receipt·roundtrip과 독립 verification/completion을 함께 보존한다. 기존 승인 55세트와 원릴리스 비교 조건을 유지했으며 실제 등록은 한 번 성공했다.\n');
console.log(JSON.stringify(result.counts));
