# runtime v6 smoke-v5 준비 인계

119세트의280물음에서249학습단위(209기준서형·40사례형),1113점을 실제 앱 함수로 투영했다. v4의 은행·분류·공개/비공개 투영 body·저장답안·criteria·배점·응답schema는 모두 그대로다.

249개 채점 요청에는 승인된 조건부 절차와 독립 부정목적에 관한 두 지시문만 추가됐다. 채점 데이터 marker부터 끝까지의 payload·답안·근거구간·출력예시는 byte 단위로 동일하다. 새 execution-runtime-v6의16코드와 이전코드 보존증거를 함께 동결했다.

빈답안249개는 명시적 빈 키로 생산 함수를 실행해 v4와 동일한0점/정상보안결과를 확인했다. 비빈답안 모델 호출은0회다. 18fixture 전부통과하며 실제 CLI dry-run 3worker 각83개와 기존출력보존/STOP처리를 확인했다. 타입검사·대상lint도 통과했다.

실행기는 v4의 manifest상수/버전만 변경했고 동작은 동일하다. 구판371개 파일은 해시로보존했다. root잠금용 전체 입력목록은 learning-unit-smoke-v5-runtime-inputs.json이며 실제 실행은 새잠금·새출력 경로로만 진행한다. 이준비는 모델 만점·전수 의미검수·DB/XP 검증이 아니다.
