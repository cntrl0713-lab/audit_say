# R3 canary B 실제 검증 및 불일치 조사

2026-09-12 KST. 대상은 `pilot-05-003` 한 세트이며 원 질문·계획·QA·은행·코드와 과거 receipt는 수정하지 않았다. 실제 API 프로세스는 모두 종료했다. remaining 및 학습 smoke는 미착수다. 이 문서는 AI의 독립 대조이며 사람 확인이나 정식 통과 receipt가 아니다.

- 의미검수: 11단위 및 생성 45개 assertion 전부 pass, 단위별 첫 응답으로 완료.
- 생성 사례 실제 채점: 같은 답안 통합 후 38개 비빈 답안과 빈 답안 1개, 총 39실행. 37일치/2불일치.
- 작성자 QA: 35개 모두 첫 실행 일치. 비빈 답안 33개와 모델 없는 빈 답안 2개. 기존 omit-crit7 원답은 5점, 보존한 법규 조건·보고 상대방 반례는 1점이다.
- 불일치 반복: 두 원답마다 추가 2회, 총 4개 실제 모델 관측과 무호출 빈 답안 2개를 별도 진단 증거로 보존했다. 두 불일치 모두 총 3회 재현됐다.
- 원시 판단 75개를 현행 순수 합산 함수로 다시 처리해 저장 결과와 전체 일치했다. 모델 없는 빈 결과 5개는 별도로 검증했다. 모든 보안 플래그는 정상이며 실제 답안 인용과 현재 prompt/schema 해시도 일치한다. 성공한 judgment 응답 75개 외 재시도·보안 재확인 trace는 없다. 의미검수 11개를 포함해 이번에 보존된 실제 유효 모델 응답은 86개다.

## 1. 적시 누락 답안의 과다 인정

원답: “감사인은 감사 중 식별된 유의적 내부통제 미비점을 지배기구에 서면으로 커뮤니케이션해야 한다.”

`sub1/crit2`의 기대 `not_met`을 유지한다. 실제는 세 번 모두 `met`, 총점 3/3/3이다. 적시 명제가 빠졌으므로 독립적으로 맞힌 지배기구·서면의 2점만 타당하다. KGA 265.9와 실제 발문은 상대방·시점·형태를 구별한다. “감사 중”은 식별에 붙은 말로 적시에 전달한다는 조치를 대신하지 않는다. raw reason이 답안에 없는 적시 의미를 인정했으므로 인용 복원·보안·합산 오류가 아닌 모델 의미판정 오류로 남긴다. 원답을 더 명시적인 답으로 대체하거나 기대값을 모델에 맞추지 않는다.

## 2. 명시적 반대 답안을 누락으로 만든 생성 기대 오류

원답: “미비점의 잠재적 영향만 설명하고, 미비점 자체의 내역은 제시하지 않는다.”

`sub2/crit4`의 원 생성 kind는 `omission`, 기대는 `not_met`이나, 실제는 세 번 모두 `contradicted`이다. KGA 265.11(a)와 포함할 내용을 묻는 발문에서 내역을 제시하지 않는다는 명시는 의무의 반대다. 잠재 영향은 독립적으로 맞아 세 번 모두 총 1점을 유지한다. 후속 기대를 `contradicted`로 정정하는 안을 제안하며, 원 생성 사례·기대·receipt는 보존한다. 0점 동률이라는 이유로 현 receipt를 통과로 바꾸지 않는다.

두 건의 원답·원기대·3회 전체 기준판정·근거·해시는 [findings.json](findings.json), 실제 반복 원시는 [진단 증거](../diagnostic-repeat-v1/observations.jsonl)에 있다. 총괄의 후속 범위 결정 전 추가 호출과 입력 변경은 하지 않는다.

로컬 재처리 검사는 `audit.mjs`로 수행했다(API 0). 최초 로컬 검사에서 빈 답안의 raw judgment가 없다는 계약을 잘못 가정해 중단했으며, 모델 없는 빈 결과는 별도 검사하도록 수정한 뒤 75개 raw 재처리와 빈 결과 5개 대조가 통과했다. 실제 모델 실행 오류는 없었다.
