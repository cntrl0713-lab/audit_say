# 실행 002 완료 뒤 필요한 후속 검증

2026-09-12. 실행 002의 전수 채점은 현재 계속 진행한다. 공통 runtime·후보·원 QA·원응답은 동결한다. **candidate-v3와 run-v2를 그대로 최종 봉인하거나 게시하지 않는다.**

## 알려진 QA 정정

- `pilot-04-007--sub3--standard--partial`: 원답은 “추가감사절차의 실시 시점과 범위만 확인한다.”이다. 원 기대 2점, 실제 0점이었다. root와 A가 KGA320.13·발문·criterion을 대조한 결과, 이 문구만으로 시기·범위가 **여전히 적합한지 결정하는 행위**를 충족한다고 확정하기 어렵다. 원답의 기대는 후속 기록에서 0점으로 정정하고 기존 기대·실측은 보존한다.
- `crit8`의 원 기대 `not_met`는 맞지만 이유가 성격의 적합성 누락이라고 잘못 적혀 있다. 실제 criterion은 수행중요성 수정 필요성 판단이므로 설명 귀속도 후속 QA에서 바로잡는다.
- 문항·모범답안·criterion·4점 배점은 유지한다. A가 보존 QA에서 명확한 부분답안을 찾아 새 역할로 제안한다. 최종 실행 003에서 그 답안만 1회 표적 검사한다. 점수를 높이기 위한 재호출이 아니라 잘못 선정된 부분답안 역할을 원문에 따라 교정하는 작업이다.
- 과거 오기대·모델 결과를 없애거나 통과율 분모에서 조용히 제외하지 않는다. 최초 고정 집합의 결과와 후속 정정 집합의 결과, 추가 호출·비용을 별도로 보고한다. 원답 0점 정정은 모델 API 재실행이 아니다.

## 다음 실행 준비

1. run-v2 세 작업자가 모두 종료될 때까지 현재 runtime을 유지한다. 추가 불일치는 내용 오류와 채점 일관성을 구별해 조사한다.
2. 원 manifest가 다른 이전 관측을 재사용할 때 해당 origin manifest와 **같은 폴더**의 `runtime-snapshots.json`을 선택하도록 consumer를 최소 수정한다. 현재 첫 suffix 일치 방식은 origin 002의 입력에 함께 보존된 001 index를 선택한다. B의 `proposed-after-run-v2/` 제안과 검사 결과를 검토한 뒤 동결 종료 후 적용한다.
3. 기존 실행 001·002의 원응답·기대값·입력·코드를 보존하고 새로운 manifest·runtime 사본·출력·실행 잠금 003을 만든다. 불변 요청은 원 manifest와 원 observation 경로를 명시해 재사용하며, 변한 대표만 새로 실행한다.
4. 내용 정정의 후속 증거를 agent review에 연결한다. old QA 오류를 5%의 채점 허용률로 면제하지 않는다.
5. 최종 738개 요청 역할·830개 평가 답안의 동일 범위를 확인한 뒤 봉인한다. 원 001에서 기대값을 정정한 `02-007 case partial` 제외 1회와 이번 `04-007 partial`의 잘못된 기대 1회도 전체 실제 호출·비용에는 포함한다.
6. C의 staging 준비 잠금은 준비 당시 runtime을 기록하므로 후속 core 변경에 대해서는 이전 준비본을 보존하고 새 준비 잠금을 만든다. 실제 모델 실행 잠금·과거 hash를 덮어쓰지 않는다.

운영 DB와 canonical은 아직 변경하지 않았다. 최종 후속 검증·봉인·staging 검사를 통과한 후 사용자가 승인한 DB 반영을 수행한다.
