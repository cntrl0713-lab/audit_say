# 효율 검증 배치 실제 API 사용량 최종 독립 집계

실제 새 요청은 **742회**, 사용량 기반 추정 비용은 **US$0.84561490**다. 001 67회 + 002 672회 + 004 3회이며 003은 dry-run 0회다. 이 보고서 작성 중 모델 API·DB 호출은 0회다.

| 실행 | 새 호출 | 입력 토큰 | 출력 토큰(추론 포함) | 추정 USD |
| --- | ---: | ---: | ---: | ---: |
| run-v1 | 67 | 201,320 | 18,676 | 0.07273115 |
| run-v2 | 672 | 2,074,805 | 210,029 | 0.76966925 |
| run-v4 | 3 | 8,319 | 946 | 0.00321450 |
| 전체 실제 사용 | 742 | 2,284,444 | 229,651 | 0.84561490 |

총 2,514,095토큰이다. 입력은 캐시 읽기 4,200, 캐시 쓰기 2,278,018, 그 외 2,226토큰이다. 추론 43,296토큰은 출력에 이미 포함되어 다시 더하지 않았다. 모든 실제 응답은 gpt-5.6-luna·default tier이며 장문 단가 적용 요청은 0개다.

| 증거 구분 | 고유 실제 응답 | 추정 USD |
| --- | ---: | ---: |
| 최종 primary | 738 | 0.84007290 |
| 교체된 과거 응답 | 4 | 0.00554200 |
| 합계 | 742 | 0.84561490 |

004의 735개 재사용 응답은 001·002에서 이미 과금된 요청이므로 새 비용으로 더하지 않았다. 최종 738개는 735개 재사용과 새 3개로 구성되며 830개 평가 대상 물음 답안을 포함한다. 제외된 과거 4개도 실제 발생 비용에는 포함했다.

- run-v1: pilot-02-007--case--partial — US$0.00167580
- run-v2: pilot-04-007--sub3--standard--partial — US$0.00119360
- run-v2: pilot-08-008--sub2--standard--partial — US$0.00136930
- run-v2: pilot-10-006--sub3--standard--partial — US$0.00130330

9개 worker summary의 new-only 합계와 742개 물리적 transport를 독립 합산했다. observation/input/transport/traces 해시, 실제 요청의 모델·prompt·schema, response ID·usage와 callback/usage.jsonl, 원응답 JSON과 trace를 전수 대조했다. response ID·request ID 중복은 없고 자동 재시도·실행 오류·누락 usage·unknown/bounded 비용은 모두 0이다. 누락 비용을 0으로 대체한 사례는 없다. 옛 001의 67개 JSON은 SDK 비열거 헤더 속성이 저장되지 않아 원 response ID·모델·usage·trace와 callback request ID의 결속으로 구분했다. 신규 675개는 명시 request_id metadata도 일치한다.

E 내 transport.jsonl 856개 중 실제 run은 742개이고 나머지 114개는 b/local-fixture-* 합성 테스트다. 실제 요청 전부가 audit_grading_judgment이므로 이 E 배치의 별도 유료 API 의미검수는 0회다. 다른 과거 검증 배치와 agent 자체 대화 사용량은 이번 범위가 아니다.

단가는 실행에 고정된 2026-09-12 가격 스냅샷(입력/캐시 읽기/캐시 쓰기/출력 1M당 $0.20/$0.02/$0.25/$1.20)이다. 이는 공급자가 반환한 토큰에 대한 산술 추정이며 청구서·세금·크레딧·계정 할인과 대조한 금액이 아니다. 읽은 3732개 근거 파일의 최종 바이트 불변을 확인했다. 내용 검토·점수 수락·게시·DB 완료를 이 집계로 대신하지 않는다.
