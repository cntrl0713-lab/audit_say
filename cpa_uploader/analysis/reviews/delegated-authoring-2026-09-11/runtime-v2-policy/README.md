# 실제 검수 정책 명확화 후속 버전

비교 은행은 `../final-153-v1/comparison-bank.json`의 153세트이며 SHA-256은 `283bb56309f913dddf7d442f72940d2896ce7f0f40cc42e3afef8cc43aad06b9`이다. 대상 문항은 49세트·131물음·433점으로 그대로다. 이 문서는 검증 완료 선언이 아니다.

## 변경 근거

- N01 T02-A의 동일 물음은 3회 의미검수에서 의도된 복습을 `fail/pass/fail`로 다르게 처리했다. 기존 ID·겹치는 요구·새 커버리지로 세지 않는 정책은 원래 계획에 있다.
- N06 T16-A의 동일 물음은 명시된 판본 가정을 `uncertain/uncertain/pass`로 다르게 처리했다. 2027 CPA 준비용 선택 판본과 향후 최종 시험 적용 공고의 확정 여부를 구별한다.
- R01 T09-A는 주어진 선행 절차를 답안에 다시 쓰도록 요구하는 해석 때문에 `fail/pass/pass`로 변동했다. 사례와 발문 전체에서 이미 주어진 조건을 적용한다.
- R01 작성자 QA에서는 무관한 완전 문장 뒤의 정상 답안에 보안 플래그가 발생했고, 명시적인 절차 불필요 주장 및 한 문장 안의 독립 명제에 대한 판정도 변동했다. 기존 정수 배점·보안 규칙의 의미를 지시에 더 명확히 적었다.

[변경 기록](change-record.json)과 두 코드의 `.before.txt`에는 원래 바이트와 변경 전후 해시를 보존한다. 모델, 점수 합산, 문항, 출처, 비교 은행은 바꾸지 않았다. [runtime-lock](runtime-lock.json)이 후속 실행의 기준이다. 이전 지시의 통과 결과를 새 지시의 통과로 재사용하지 않는다.

## 후속 QA

N01 T02-A의 반대 답안 2개는 대상 외의 명제까지 부정할 수 있었다. [후속 QA](../../../../drafts/delegated-authoring-2026-09-11/n01/phase-two-followup/qa-v2/qa-cases-t02-a.json)는 49개 사례와 점수를 유지하며 이 2개 답안만 대상 명제의 명시적 반대로 좁힌다. 원본 QA·실측은 보존하고 원래 넓은 답안은 탐색용 회귀 자료로 별도 보존한다. 전체 작성자 QA 수는 2,389개다.

## 검증 상태

정책 변경 뒤 관련 31개 코드 검사는 통과했다. 이 검사는 주입 응답·형상·전달 계약 검사이며 실제 모델의 의미 정확성 검증이 아니다. 실제 의미검수와 작성자 QA는 새 경로에서 실행하고, 기존 실패·수정 근거·재검수 결과를 함께 보존한다. 각 담당자의 2차 인계를 수집한 뒤 전체 처리 범위를 다시 집계한다.

모든 초안은 `needs_review / needs_human_review`를 유지한다. 정본 편입·공개본 생성·게시·DB 반영·배포는 수행하지 않는다.
