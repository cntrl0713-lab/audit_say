---
description: Strict planning entrypoint; use the repository .agents skill as the canonical procedure
---

# Strict plan entrypoint

Use `.agents/skills/plan-strict/SKILL.md` as the single canonical procedure for this
workflow. It contains the required slices, domain risks, verification steps, and output
format. Do not duplicate or override that procedure here.
