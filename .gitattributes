# This repository stores every text file with LF. Without this, core.autocrlf=true
# rewrites the working tree to CRLF on checkout, which leaves `git status` permanently
# dirty and breaks the question generation tests that read wiki sources verbatim.
* text=auto eol=lf

.githooks/* text eol=lf
