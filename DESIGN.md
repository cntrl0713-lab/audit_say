# AuditSay 디자인 시스템

회계감사 서술형 학습과 회계법인·채용 탐색을 위한 디자인 계약이다. 기존 Cursor Orange 참고안을 대체한다.

## 색상과 타이포

구현 정본은 `app/globals.css`다. 브랜드 원색은 딥 네이비 `#1E2D4D`, 라이트 배경은 `#F7F5F0`, 카드 배경은 흰색이다. 다크 배경은 `#111827`, 카드 배경은 `#192235`다. 네이비를 본문·배경에 기계적으로 반복하지 않고 `primary`, `primary-foreground`, `muted`, `surface-soft` 등 의미별 토큰을 쓴다.

다크 모드의 주요 버튼은 밝은 네이비 계열 `#C2D2F1`과 진한 글자 `#14213A`를 조합한다. 버튼 글자에는 `text-primary-foreground`를 사용한다. 차트는 `--chart-1`~`--chart-6`을 사용하며 출처의 수치·비교 조건은 스타일 변경으로 바꾸지 않는다.

Noto Sans KR을 Next.js 폰트로 제공한다. 본문·답안은 16px, 긴 지문은 행간 2, 제목은 600, 보조 정보는 12~14px을 기본으로 한다. 4px 간격 단위, 버튼 8px·패널 14px 모서리, 최소 44px 주요 터치 영역을 사용한다. 장식용 그림자와 모션은 제한하고 감소된 모션 설정을 따른다.

## 내비게이션과 화면

- 상위: 문제 풀이 / 회계법인 / 내 학습. 계정 메뉴와 테마 전환은 우측에 둔다.
- 회계법인: 회계법인 정보 / 채용공고. 정보 아래에 법인 목록·리포트·챗봇·평점을 유지한다. `/firms`, `/jobs`와 법인별 공고 주소를 유지한다.
- 내 학습: 커리큘럼·풀이 기록·오답노트·랭킹. 계정 메뉴: 내 정보·알림 설정·계정 관리·세법학·로그아웃 및 관리자 진입점.
- `/`는 공개 랜딩, `/login`은 기존 통합 계정 로그인·회원가입·비회원 시작과 로그인 후 학습 진입이다.
- 랜딩: 학습 가치와 화면 예시 → 채점 흐름 → 학습 기능 → 회계법인·채용 → 이용 안내.
- 사례형: 1,024px 이상에서 왼쪽 사례 44%·오른쪽 물음과 답안 56%를 기본으로 하고 사례 폭 35~55% 조절을 제공한다. 오른쪽에 물음 이동·발문·답안 입력란을 순서대로 둔다. 좁은 화면에서는 사례 / 답안 작성 / 채점 결과를 전환한다.
- 답안 입력란: 기본 5줄로 시작하고 수동 세로 조절과 확대 버튼을 제공한다. 확대는 입력란 높이만 늘려 PC의 왼쪽 사례를 계속 볼 수 있게 한다.
- 기준서형: 독립 발문과 답안에 집중하는 중앙 화면. 사례를 붙이지 않는다.
- 물음 이동과 읽기·답안 탭 전환은 입력 DOM을 유지한다. 주제로 찾은 사례에서도 모든 사례형 물음을 유지하고 기존 전체 제출 범위를 명시한다.
- 결과: 원점수·기준별 판정·원답안 인용·접을 수 있는 모범답안·회원 복습 진입점. 기존 채점 결과를 표시하며 별도 AI 요약이나 추정 성적을 만들지 않는다.

## 상태와 보존

테마 기본값은 라이트다. 라이트 / 다크 / 시스템을 전환하고 브라우저에 저장하며 사용자가 저장한 선택은 유지한다. 저장값이 없거나 유효하지 않으면 OS 설정과 관계없이 라이트를 적용한다. 초기 페인트 전에 테마를 적용하며 시스템 변경과 다른 탭의 설정 변경을 반영한다. 저장소를 쓸 수 없어도 현재 화면 전환은 동작한다.

작성 중 답안은 `sessionStorage`에 사용자·학습 단위·판본·공개 발문과 사실을 결속해 저장한다. 7일이 지난 값, 다른 사용자·판본·물음의 값은 복원하지 않는다. 탭을 닫으면 사라질 수 있음을 표시한다. 기존 서명된 제출 복원 정보와 별개이며 저장 실패는 화면에 안내하고 입력을 유지한다. URL에 선택한 학습 단위를 보존해 새로고침 후 다시 연다.

문항 내용·정답·배점·채점 모델·서버 제출 계약은 UI 변경으로 수정하지 않는다. 모범답안은 서버의 채점 결과가 있을 때만 표시한다. 채점 오류를 0점으로 바꾸지 않는다.

## 콘텐츠 원칙

화면 예시의 점수는 예시임을 명시한다. 공개 저장소, 공식 인증, 보안 수준, 실제로 제공되지 않는 가격·추천 기능을 임의로 주장하지 않는다. 이용약관·개인정보처리방침·문의 링크는 운영자가 확정한 문서와 연락처가 준비되면 연결한다. 법적 정책을 UI 작업에서 임의 작성하지 않는다.
